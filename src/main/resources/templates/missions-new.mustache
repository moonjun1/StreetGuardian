<!DOCTYPE HTML>
<html lang="ko">
<head>
    <title>게시판 작성 - Street Guardian</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background-color: #f8f9fa;
            color: #343a40;
        }

        .header {
            text-align: center;
            padding: 20px;
            background: linear-gradient(135deg, #6c5ce7, #a8a4e6);
            color: white;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 100;
        }

        .form-container {
            margin-top: 80px; /* 헤더 높이만큼 여백 추가 */
            padding: 20px;
            max-width: 600px; /* 최대 너비 설정 */
            margin-left: auto; /* 중앙 정렬 */
            margin-right: auto; /* 중앙 정렬 */
        }

        .form-card {
            background: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        .form-card h2 {
            margin-bottom: 15px;
            color: #6c5ce7; /* 제목 색상 */
        }

        .form-group {
            margin-bottom: 15px; /* 각 입력 그룹 간의 간격 */
        }

        .form-group label {
            display: block; /* 레이블을 블록으로 설정하여 줄 바꿈 */
            margin-bottom: 5px; /* 레이블과 입력 필드 간의 간격 */
        }

        .form-group input,
        .form-group textarea {
            width: 100%; /* 전체 너비 사용 */
            padding: 10px; /* 내부 여백 */
            border-radius: 5px; /* 모서리 둥글게 */
            border: 1px solid #ccc; /* 테두리 색상 */
        }

        .form-group textarea {
            resize: vertical; /* 세로 방향으로만 크기 조절 가능 */
        }

        .submit-btn {
            background-color: #6c5ce7; /* 버튼 배경색 */
            color: white; /* 버튼 글자색 */
            border: none; /* 테두리 없음 */
            padding: 10px 20px; /* 내부 여백 */
            border-radius: 5px; /* 모서리 둥글게 */
            cursor: pointer; /* 포인터 커서 변경 */
            transition: background-color 0.3s ease; /* 호버 효과 애니메이션 */
        }

        .submit-btn:hover {
            background-color: #5f4dd0; /* 호버 시 색상 변경 */
        }

    </style>
</head>
<body>
<div class="header">
    <h1>미션 게시판</h1>
</div>

<div class="form-container">
    <div class="form-card">
        <h2>미션 내용 입력</h2>

        <form action="/missions-submit" method="POST"> <!-- 제출할 URL -->
            <div class="form-group">
                <label for="title">제목</label>
                <input type="text" id="title" name="title" required placeholder="게시판 제목을 입력하세요.">
            </div>

            <div class="form-group">
                <label for="content">내용</label>
                <textarea id="content" name="content" rows="5" required placeholder="게시판 내용을 입력하세요."></textarea>
            </div>

            <button type="submit" class="submit-btn">작성하기</button> <!-- 제출 버튼 -->
        </form>
    </div>
</div>

</body>
</html>
